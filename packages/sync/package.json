{
  "name": "@atlas-baobab/sync",
  "version": "0.1.0",
  "description": "Sync offline-first : horloge hybride (HLC), fusion last-write-wins par champ, outbox. CDC 2.4.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vitest": "^2.1.8"
  }
}
